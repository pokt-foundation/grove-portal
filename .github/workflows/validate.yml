name: 🔬 Validate
on:
  workflow_call:
  pull_request: {}

defaults:
  run:
    shell: bash

jobs:
  validate:
    name: ⬣ Validate
    runs-on: ubuntu-latest
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      - name: ⎔ Setup node
        uses: actions/setup-node@v3
        with:
          node-version: 16.15.0

      - uses: pnpm/action-setup@v2.0.1
        with:
          version: 7.0.0

      - name: 👀 Env
        run: |
          echo "Event name: ${{ github.event_name }}"
          echo "Git ref:    ${{ github.ref }}"
          echo "GH actor:   ${{ github.actor }}"
          echo "SHA:        ${{ github.sha }}"
          VER=`node --version`; echo "Node ver: $VER"
          VER=`pnpm --version`; echo "pnpm ver: $VER"

      - name: create env file
        run: |
          touch .env
          echo NODE_ENV="development" >> .env
          echo BACKEND_URL=https://backend.staging.portal.pokt.network >> .env
          echo AMPLITUDE_API_KEY=4b54fb07298c6322918050a273203084 >> .env
          echo SENTRY_DSN=https://0f9f40d0428b4c31aa94cb516159ec42@o515876.ingest.sentry.io/5631627 >> .env
          echo SESSION_SECRET=pokt_network_engineering >> .env
          echo AUTH0_DOMAIN=dev-f8fl5e69.us.auth0.com >> .env
          echo AUTH0_CLIENT_ID=TJykObV0ZyzCuggvsZXmcCpKARLtEblg >> .env
          echo AUTH0_CLIENT_SECRET=R4x2EbixEFkvdWR2h_AD3F7GE3e2yU0RZtka1KqjH-2GKNP88oPWiRUGtgRij91E >> .env
          echo AUTH0_AUDIENCE=https://dev-f8fl5e69.us.auth0.com/api/v2/ >> .env
          echo AUTH0_SCOPE=read:current_user update:current_user_metadata openid profile email >> .env

      - name: 📥 Download deps
        run: pnpm install

      - name: 🔬 Lint
        run: pnpm run lint

      - name: 🔎 Type check
        run: pnpm run typecheck

      - name: ⚡ Run vitest
        run: pnpm run test:unit:run

      - name: 🌳 Cypress run
        run: pnpm run test:e2e:run

      - name: 🏗 Build
        run: pnpm run build
